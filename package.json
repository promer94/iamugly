{
  "name": "html5css3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass-build": "node-sass-chokidar src/ -o src/",
    "build": "postcss src/**/**/*.css src/**/*.css --replace --use autoprefixer -m",
    "watch-sass": "npm run sass-build && node-sass-chokidar src/ -o src/ --watch --recursive"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^9.1.5",
    "node-sass-chokidar": "^1.3.3",
    "postcss-cli": "^6.0.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
