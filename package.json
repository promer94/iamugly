{
  "name": "html5css3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node-sass-chokidar src/ -o src/",
    "postcss": "postcss src/**/*.css --replace --use autoprefixer -m",
    "watch": "npm run build && node-sass-chokidar src/ -o src/ --watch --recursive"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^9.1.5",
    "node-sass-chokidar": "^1.3.3",
    "postcss-cli": "^6.0.0"
  }
}
